name: sync-to-cnb
on:
  - push
  - delete

jobs:
  call-cnb-sync:
    uses: TDesignOteam/workflows/.github/workflows/reusable-cnb-sync.yml@main
    with:
      github-repo: liweijie0812/tdesign-skills
      cnb-repo: liweijie0812/tdesign-skills
    secrets:
      CNB_TOKEN: ${{ secrets.CNB_TOKEN }}
